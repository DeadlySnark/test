;-----------------------------------------------------------
;	450,カメラ設置
;-----------------------------------------------------------
@COM450
	;内容を初期化して設置処理
	TSTR:10 = 

	;設置場所をMASTERの現在地に
	CFLAG:MASTER:カメラ設置場所 = CFLAG:MASTER:現在位置
	;設置時刻を取得
	CFLAG:MASTER:カメラ設置時刻 = TIME + (DAY * 60 * 24)
	;記録回数の初期化
	CFLAG:MASTER:カメラ記録回数 = 0

	;経験上昇処理
	;未実装
	;ABL:MASTER:設置経験 ++

RETURN 1
