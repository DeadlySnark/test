;-------------------------------------------------
;訪問者と会話
;-------------------------------------------------
@COM460
PRINTFORML 何を話そうか？
PRINTFORML [0] 何か話をして欲しい
SIF FLAG:22 > 10 && CFLAG:MASTER:屈服度 >= 1000
	PRINTFORML [1] キスして欲しい
PRINTFORML [2] お互いについて
SIF CFLAG:MASTER:屈服度 >= 1000
	PRINTFORML [100] 抱いて欲しい
SIF CFLAG:MASTER:屈服度 >= 1000
	PRINTFORML [200] 紅魔館に戻りたい

$INPUT_LOOP_COM460
INPUT
IF RESULT == 0
	TRYCALL COM417_TALK
ELSEIF RESULT == 1
	SIF FLAG:22 <= 10 || CFLAG:MASTER:屈服度 < 1000
		GOTO INPUT_LOOP_COM460
	TRYCALL COM417_KISS
ELSEIF RESULT == 2
	CALL COM460_ABOUT_TWO
ELSEIF RESULT == 100
	SIF CFLAG:MASTER:屈服度 < 1000
		GOTO INPUT_LOOP_COM460
	;CALL COM417_LOSE(900,FLAG:22,FLAG:23)
ELSEIF RESULT == 200
	SIF CFLAG:MASTER:屈服度 < 1000
		GOTO INPUT_LOOP_COM460
	CALL COM460_RETURN
ENDIF
TRYCALL NTR_HOME_OUT
TRYCALL NTR_BEDIN
RETURN 0

;無いとは思うけどある要望を出された時用に処理を別々に
@COM460_ABOUT_TWO
PRINTFORMW お互いについてどう思ってるか正直に話す事にした
PRINTFORM %NTR_NAME(0)%は%CALLNAME:MASTER%の事を
IF CFLAG:MASTER:屈服度 < 200
	PRINTW 調教対象としか思っていないようだ
ELSEIF CFLAG:MASTER:屈服度 < 400
	PRINTW 調教対象の中では気になってきているようだ
ELSEIF CFLAG:MASTER:屈服度 < 600
	PRINT 調教対象の中では
	;性格と体型で判別
	LOCAL = TALENT:MASTER:10 + TALENT:MASTER:11 + TALENT:MASTER:12 + TALENT:MASTER:13 + TALENT:MASTER:100
	IF LOCAL <= 0
		PRINT 可愛い
	ELSE
		PRINT 綺麗だ
	ENDIF
	PRINTW と思っているようだ
ELSEIF CFLAG:MASTER:屈服度 < 800
	PRINT 調教対象の中でも特別
	;性格と体型で判別
	LOCAL = TALENT:MASTER:10 + TALENT:MASTER:11 + TALENT:MASTER:12 + TALENT:MASTER:13 + TALENT:MASTER:100
	IF LOCAL <= 0
		PRINT 可愛い
	ELSE
		PRINT 綺麗だ
	ENDIF
	PRINTW と思っているようだ
ELSEIF CFLAG:MASTER:屈服度 < 1000
	PRINT 一人の女性として
	;性格と体型で判別
	LOCAL = TALENT:MASTER:10 + TALENT:MASTER:11 + TALENT:MASTER:12 + TALENT:MASTER:13 + TALENT:MASTER:100
	IF LOCAL <= 0
		PRINT 可愛い
	ELSE
		PRINT 綺麗だ
	PRINTW と思っているようだ
	ENDIF
ELSE
	;性格と体型で判別
	LOCAL = TALENT:MASTER:10 + TALENT:MASTER:11 + TALENT:MASTER:12 + TALENT:MASTER:13 + TALENT:MASTER:100
	IF LOCAL <= 0
		PRINT 可愛い
	ELSE
		PRINT 綺麗な
	ENDIF
	PRINTW 恋人のようなものだと思っているようだ
ENDIF
IF CFLAG:MASTER:屈服度 < 200
	PRINTFORMW %CALLNAME:MASTER%も%NTR_NAME(0)%の事を非道い強姦魔だと罵った
	PRINTW 少し雰囲気が悪くなった
	FLAG:22 -= RAND:10 + 1
	CFLAG:MASTER:屈服度 -= 2
ELSEIF CFLAG:MASTER:屈服度 < 400
	PRINTFORMW %CALLNAME:MASTER%は%NTR_NAME(0)%の事を強姦魔だと言った
	PRINTW 少し雰囲気が悪くなった
	FLAG:22 -= RAND:10 + 1
	CFLAG:MASTER:屈服度 -= 2
ELSEIF CFLAG:MASTER:屈服度 < 600
	PRINTW 喜んで良いのか悪いのかわからない
ELSEIF CFLAG:MASTER:屈服度 < 800
	PRINTFORMW %CALLNAME:MASTER%は少し照れ臭くなり視線を逸らした
	PRINTW 雰囲気が少し良くなった
	FLAG:22 += RAND:10 + 1
	CFLAG:MASTER:屈服度 += 2
ELSEIF CFLAG:MASTER:屈服度 < 1000
	PRINTFORMW %CALLNAME:MASTER%は素直に喜んだ
	PRINTW 雰囲気が少し良くなった
	FLAG:22 += RAND:10 + 1
	CFLAG:MASTER:屈服度 += 2
ELSE
	PRINTFORMW %CALLNAME:MASTER%も同じ風に思ってると答えた
	PRINTW 雰囲気が良くなった
	FLAG:22 += RAND:10 + 2
	CFLAG:MASTER:屈服度 += 4
ENDIF
TIME += 10
RETURN 0

@COM460_RETURN
PRINTFORML %CALLNAME:MASTER%がそろそろ紅魔館に戻りたいと告げると%NTR_NAME(0)%は鍵を開け、外に出してくれた
CFLAG:MASTER:現在位置 = 1
FLAG:22 = 0
FLAG:23 = 0
FLAG:24 = 0
FLAG:25 = 0
