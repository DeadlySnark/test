;--------------------------------------------------
;汎用メッセージ関数群
;何か引数から文字列を取得できる関数など
;--------------------------------------------------

;壁または木取得
@MSG_WALL_OR_TREE
#FUNCTIONS
IF FLAG:訪問者の現在位置 == 16 || FLAG:訪問者の現在位置 == 20
	RETURNF "木"
ELSE
	RETURNF "壁"
ENDIF

;割れ目描写文字列取得
@MSG_SLIT(ARG)
#FUNCTIONS
IF TALENT:ARG:体型 < 0
	IF RAND:2 == 0
		RETURNF "可憐なスリット"
	ELSE
		RETURNF "幼いワレメ"
	ENDIF
ELSE
	IF RAND:2 == 0
		RETURNF "秘めやかな花弁"
	ELSE
		RETURNF "秘裂"
	ENDIF
ENDIF
